# プロダクト仕様書: Kusabi (楔)

> **The AI-Native Metarepo Connector for Git.**  
> 複数のリポジトリを、単一の文脈（Context）として繋ぎ止める軽量メタリポジトリ・コネクタ

---

## 1. ブランド設計

| 項目 | 定義 |
| :--- | :--- |
| **プロダクト名** | **Kusabi**（楔） |
| **CLI コマンド** | `kusabi` （`git-kusabi` として `git kusabi` にも対応。エイリアス: `ksb`） |
| **コードネーム** | **Wedge** |
| **コンセプト** | 親のGit履歴を汚さず、子レポのポインタと「役割（Role）」だけを一本に束ねる軽量メタリポジトリ・コネクタ |
| **キャッチコピー** | *Connect Repositories. Unify Contexts.* |
| **ターゲット** | 複数リポジトリ（マイクロサービス/マルチアプリ）で開発し、AIエージェント（Cursor, Claude Code, Windsurf等）をフル活用する開発者 |

---

## 2. ディレクトリ構造

```plaintext
my-project-meta/               <-- 親メタリポジトリ (Kusabiで管理)
├── kusabi.yaml                <-- マニフェストファイル（必須）
├── AGENTS.md                  <-- 全体共通のAI行動指針・アーキテクチャ定義
├── .gitignore                 <-- Kusabiが自動生成・管理 (子レポの除外)
└── packages/                  <-- .gitignore で自動除外（子レポの実体）
    ├── app-ios/               <-- 個別のGitリポジトリ (.git 独立)
    └── app-backend/           <-- 個別のGitリポジトリ (.git 独立)
3. マニフェスト仕様 (kusabi.yaml)
YAML
version: "1"
name: "my-ecosystem"
description: "Cross-platform ecosystem bound by Kusabi."

# AI連携の全体設定
context:
  agents: "./AGENTS.md"
  # 各レポ内から集約するAIドキュメントのパターン (例: 各レポの CLAUDE.md を読み込む)
  includes:
    - "README.md"
    - "CLAUDE.md"

# 束ねるリポジトリの定義
repositories:
  app-ios:
    path: "packages/app-ios"
    url: "git@github.com:org/app-ios.git"
    role: "iOS Client App (Swift / SwiftUI)"
    tags: ["frontend", "ios"]

  app-backend:
    path: "packages/app-backend"
    url: "git@github.com:org/app-backend.git"
    role: "Core API Server (Go / gRPC)"
    tags: ["backend", "api"]
4. CLI インターフェース仕様
基本コマンド一覧
Bash
# 1. 初期化 (kusabi.yaml & AGENTS.md テンプレートの生成)
kusabi init

# 2. 全レポの同期 (未クローンの引き込み & .gitignoreの自動設定)
kusabi sync [--depth=1]

# 3. 全レポのステータス確認 (ブランチ、未コミット変更の一覧)
kusabi status

# 4. 一括コマンド実行 (並列処理)
kusabi exec "git status -s"
kusabi exec --tag ios "xcodebuild test -scheme App"

# 5. AI用コンテキストの抽出 (STDOUT出力)
kusabi context | pbcopy
kusabi context --tree    # ディレクトリ構成も含めて出力
kusabi context --json    # Structured JSONで出力

# 6. リポジトリの追加 / 削除 (kusabi.yaml のCLI操作)
kusabi add app-web git@github.com:org/app-web.git --path packages/app-web --role "Web Frontend"
kusabi remove app-web

# 7. 健全性チェック (.gitignoreの不整合や未クローン状態を検出)
kusabi doctor
5. kusabi context 出力仕様（ブラッシュアップ版）
AIエージェントへ提示するコンテキストの品質を高めるため、各レポの構成情報と個別README/CLAUDE.mdを動的に統合出力します。

Markdown
# Project Context (Generated by Kusabi)

## Meta Architecture & Global Policy
[親の AGENTS.md の内容がここに注入される]

## Bound Repositories Overview
- **app-ios** (`packages/app-ios`)
  - Role: iOS Client App (Swift / SwiftUI)
  - Tags: frontend, ios
- **app-backend** (`packages/app-backend`)
  - Role: Core API Server (Go / gRPC)
  - Tags: backend, api

---

## Repository Contexts

### 1. app-ios (`packages/app-ios`)
> Role: iOS Client App (Swift / SwiftUI)

[packages/app-ios/README.md または CLAUDE.md の内容がここに自動挿入される]

### 2. app-backend (`packages/app-backend`)
> Role: Core API Server (Go / gRPC)

[packages/app-backend/README.md または CLAUDE.md の内容がここに自動挿入される]

---

## Instructions for AI Agents
1. You are working within a metarepo managed by Kusabi.
2. When implementing or modifying features, navigate directly to the target repository path (`packages/<repo-name>`).
3. Commit and Git operations must be performed within each specific sub-repository directory.
6. README テンプレート
Plaintext
  _  ___  _ ____    _    ____ ___
 | |/ / || |  _ \  / \  | __ )_ _|
 | ' /| || | |_) |/ _ \ |  _ \| |
 | . \| || |  _ </ ___ \| |_) | |
 |_|\_\\___/|_| \_/_/   \_\____/___|

Connect repositories. Unify contexts.
Kusabi is a lightweight CLI that binds multiple Git repositories 
into a seamless, AI-native metarepo without Submodule friction.

Features:
- 🚀 Zero-Lock Git Management (Auto .gitignore & Parallel sync)
- 🧠 AI-Context Aggregator (Inject whole meta-structure into LLMs)
- ⚡ High-Performance Parallel Execution (Written in Go)

---